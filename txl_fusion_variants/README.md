# Shared TXL Fusion pipeline

This folder keeps only the shared pipeline code required by the final PCA50 TXL release.

The exploratory variant configuration folders from the research workspace are intentionally not included in this GitHub release. The final manuscript model is available under `txl_model_pca50/`.
