# llm_pca_50

Held-out-only inference for the semantic-only PCA-50 baseline. It is pinned to the third physical GPU via `CUDA_VISIBLE_DEVICES=2` by default.

Run it with:

```bash
python /path/to/3_classes_classification/weighted_version/inference/llm_pca_50/heldout_llm_pca50_inference.py
```
