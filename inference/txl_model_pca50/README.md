Held-out test inference for the standalone PCA50 TXL model.

Run with:

```bash
cd /path/to/3_classes_classification/weighted_version/inference/txl_model_pca50
python heldout_txl_model_pca50_inference.py
```
